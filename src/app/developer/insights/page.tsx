import { PlaceholderPage } from '@/components/dashboard/placeholder-page';
import { Brain } from 'lucide-react';

export default function AIInsights() {
  return <PlaceholderPage title="AI Insights" icon={Brain} />;
}
