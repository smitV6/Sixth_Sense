import { PlaceholderPage } from '@/components/dashboard/placeholder-page';
import { Settings } from 'lucide-react';

export default function Profile() {
  return <PlaceholderPage title="Profile" icon={Settings} />;
}
